const colors = ['Red', 'Green', 'Blue'];

// Old For-In for array "[]"
// for (let index in colors) console.log(index, colors[index]);

// New For-Of for array
for (let color of colors) console.log(color);
